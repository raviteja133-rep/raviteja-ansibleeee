- name: installing nginx
  host: web
  become: yes

  task:
    - name: install nginx
      ansible.builtin.dnf:
        name: nginx
        state: installed

    - name: start and enable nginx
      ansible.builtin.service:
        name: nginx
        state: restarted
        enabled: yes    